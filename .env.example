# ==========================================
# Garment ERP - 环境变量配置示例
# ==========================================

# --- 数据库配置 (必需) ---
# PostgreSQL 连接字符串
DATABASE_URL=postgresql://user:password@host:5432/garment_erp?schema=public

# --- JWT 配置 (必需) ---
# JWT 密钥 - 必须设置，最少 32 个字符
# 生成命令: openssl rand -base64 32
JWT_SECRET=your-long-random-string-min-32-chars-required

# --- 应用配置 ---
# 设置为 true 启用 HTTPS (生产环境必需)
USE_HTTPS=false

# 设置为 true 运行数据库种子 (仅首次运行)
ALLOW_SEED=false

# --- 可选: AfterShip API 配置 ---
# 用于自动物流跟踪 (UPS, FedEx, DHL)
# 免费版: 100 次跟踪/月
# 获取密钥: https://www.aftership.com
# AFTERSHIP_API_KEY=asat_your_key_here

# --- 可选: 腾讯云 COS 配置 ---
# 用于文件存储
# COS_SECRET_ID=your-secret-id
# COS_SECRET_KEY=your-secret-key
# COS_REGION=ap-guangzhou
# COS_BUCKET=your-bucket-name
