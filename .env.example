# --- Replit Secrets (set via lock icon, NOT here) ---
# DATABASE_URL is auto-provided by Replit Postgres. Only set manually for external DBs.
# DATABASE_URL="postgresql://user:password@host:5432/garment_erp?schema=public"

# REQUIRED — app will refuse to start without this
# JWT_SECRET="your-long-random-string-min-32-chars"

# Set to "true" ONCE to run seed, then remove
# ALLOW_SEED="true"

# Optional — AfterShip API key for automatic carrier tracking (UPS, FedEx, DHL)
# Free tier: 100 trackings/month. Get a key at https://www.aftership.com
# AFTERSHIP_API_KEY="asat_your_key_here"
